@model WorkAcademy.Models.Empresa
@{
    ViewData["Title"] = "Cadastrar Empresa";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-9 col-xl-8">

            <div class="card shadow-lg border-0 rounded-3">
                <div class="card-header d-flex align-items-center justify-content-between bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="fas fa-building me-2"></i>
                        Cadastrar Empresa
                    </h4>
                    <a asp-action="Index" class="btn btn-sm btn-light">
                        <i class="fas fa-arrow-left me-1"></i> Voltar
                    </a>
                </div>

                <div class="card-body">

                    @if (TempData["Erro"] != null)
                    {
                        <div class="alert alert-danger"><i class="fas fa-triangle-exclamation me-2"></i>@TempData["Erro"]</div>
                    }
                    @if (TempData["Sucesso"] != null)
                    {
                        <div class="alert alert-success"><i class="fas fa-check-circle me-2"></i>@TempData["Sucesso"]</div>
                    }

                    <div class="alert alert-info d-flex align-items-start">
                        <i class="fas fa-circle-info me-2 mt-1"></i>
                        <div>
                            Para publicar vagas, conclua o cadastro da sua empresa. Você poderá editar esses dados depois.
                        </div>
                    </div>

                    <form asp-controller="Empresas" asp-action="Create" method="post" class="mt-3">
                        @Html.AntiForgeryToken()

                        <div asp-validation-summary="ModelOnly" class="alert alert-danger d-none"></div>

                        <!-- Vincula a empresa ao usuário logado (servidor também reforça) -->
                        <input type="hidden" asp-for="IdentityUserId"
                               value="@User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value" />

                        <!-- SEÇÃO 1: Identidade da Empresa -->
                        <div class="border rounded-3 p-3 mb-3">
                            <div class="d-flex align-items-center mb-2">
                                <i class="fas fa-id-card me-2 text-primary"></i>
                                <h6 class="mb-0 text-primary">Identidade da Empresa</h6>
                            </div>

                            <!-- Se quiser campos personalizados, troque o EditorForModel pelos inputs específicos aqui -->
                            @Html.EditorForModel()
                        </div>

                        <!-- SEÇÃO 2: Dicas rápidas -->
                        <div class="bg-light rounded-3 p-3 mb-3">
                            <div class="d-flex align-items-center mb-2">
                                <i class="fas fa-lightbulb me-2 text-warning"></i>
                                <strong>Dicas para um bom cadastro</strong>
                            </div>
                            <ul class="mb-0 ps-3">
                                <li>Use o nome fantasia que os candidatos reconhecem.</li>
                                <li>Inclua site, redes e formas de contato (se existirem no modelo).</li>
                                <li>Preencha descrição clara da empresa (missão, produtos, stack, benefícios).</li>
                                <li>Endereço: “Remoto”, “Híbrido” ou cidade/estado.</li>
                            </ul>
                        </div>

                        <div class="d-flex justify-content-end gap-2">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="fas fa-xmark me-1"></i> Cancelar
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save me-1"></i> Salvar e criar vaga
                            </button>
                        </div>
                    </form>
                </div>

                <div class="card-footer text-muted small">
                    Após salvar, você será direcionado para criar sua primeira vaga.
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
